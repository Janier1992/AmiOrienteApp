/**
 * =============================================================================
 * ARCHIVO DE SETUP PARA TESTS
 * =============================================================================
 * 
 * Este archivo se ejecuta antes de cada suite de tests.
 * Configura matchers de jest-dom y mocks globales.
 * =============================================================================
 */

import '@testing-library/jest-dom';

// Mock de variables de entorno si es necesario
// vi.stubGlobal('import.meta', { env: { ... } });
